const bcypt = require('bcrypt')
const jwt = require('jsonwebtoken')

exports.Register = async (req, res) => {
  try {
    const {name, gender, email, password, phone, role}
  } catch (error) {
    
  }
}